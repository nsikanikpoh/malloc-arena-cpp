#include <iostream>
#include <sstream>
#include <cstdint>

#include <Malloc.hpp>

void usage() {
    std::cerr << "Usage: factorial <N>" << std::endl;
    exit(1);
}

int mainImpl(int argc, const char* argv[]) {
    
    return 0;
}